apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infrastructure
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/robin-vidal/homelab
        revision: main
        directories:
          - path: kubernetes/infrastructure/*
  template:
    metadata:
      name: "infra-{{path.basename}}"
    spec:
      project: default
      source:
        repoURL: https://github.com/robin-vidal/homelab
        targetRevision: main
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{path.basename}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
