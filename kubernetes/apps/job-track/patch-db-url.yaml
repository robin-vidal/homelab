apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-track
spec:
  template:
    spec:
      containers:
        - name: bot
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: job-track-db-app
                  key: uri
