# Homepage

Homelab dashboard (gethomepage.dev). Search bar (Google, `-ai` appended to every query), quick links to services.

Auto-discovery via Kubernetes annotations is possible but requires `enableRbac: true` and Ingress resources with `gethomepage.dev/*` annotations.
