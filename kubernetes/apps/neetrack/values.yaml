existingSecret: neetrack-db-app

app:
  listenAddr: ":8080"
  cookieSecure: "true"
  logLevel: "INFO"

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 250m
    memory: 128Mi
