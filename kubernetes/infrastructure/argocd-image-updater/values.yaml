config:
  logLevel: info
  # Read Argo CD Application CRs directly from the API server (no Argo CD API token).
  applicationsAPIKind: kubernetes
  gitCommitUser: argocd-image-updater
  gitCommitMail: image-updater@users.noreply.github.com
