# CloudNativePG

PostgreSQL operator for Kubernetes, deployed via the official CloudNativePG Helm chart. Manages all app databases in this cluster. Each app provisions its own `Cluster` CR in its own namespace.
