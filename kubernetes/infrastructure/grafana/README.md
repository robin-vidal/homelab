# Grafana

Dashboards and visualization. Wired to the `prometheus` component in this same `infrastructure/` folder via a hardcoded datasource URL (`infra-prometheus-server.monitoring`). If the Prometheus release name changes, update the datasource URL here.
